on_actions = {
  on_monthly_SOV = {
    effect = {

      if = {
        limit = {
          tag = SOV
          has_completed_focus = SOV_evolve_our_urban_centres
          NOT = { has_country_flag = wa_mp_fix_sov_30civs_done }
        }
        set_country_flag = wa_mp_fix_sov_30civs_done
        country_event = { id = wa_mp_fix_sov_industry.1 }
      }

      if = {
        limit = {
          tag = SOV
          has_completed_focus = SOV_extend_the_working_week
          NOT = { has_country_flag = wa_mp_fix_sov_30mils_done }
        }
        set_country_flag = wa_mp_fix_sov_30mils_done
        country_event = { id = wa_mp_fix_sov_industry.2 }
      }

      if = {
        limit = {
          tag = SOV
          has_completed_focus = SOV_a_secure_industrial_base
          NOT = { has_country_flag = wa_mp_fix_sov_inf_done }
        }
        set_country_flag = wa_mp_fix_sov_inf_done
        country_event = { id = wa_mp_fix_sov_industry.3 }
      }

      if = {
        limit = {
          tag = SOV
          has_completed_focus = SOV_start_the_third_five_year_plan
          NOT = { has_country_flag = wa_mp_fix_sov_south_mils_done }
        }
        set_country_flag = wa_mp_fix_sov_south_mils_done
        country_event = { id = wa_mp_fix_sov_industry.4 }
      }

    }
  }
}

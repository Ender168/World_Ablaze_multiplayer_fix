on_monthly = {
  effect = {
    if = {
      limit = {
        date >= 1938.12.31
        NOT = { has_idea = wa_mp_fix_land_mastery_boost }
      }
      add_ideas = wa_mp_fix_land_mastery_boost
    }
  }
}

on_actions = {
  on_monthly_SOV = {
    effect = {
      if = {
        limit = {
          has_completed_focus = SOV_pc_of_mechanical_engineering
          NOT = { has_country_flag = wa_mp_fix_sov_pc_of_mechanical_engineering_done }
        }
        set_country_flag = wa_mp_fix_sov_pc_of_mechanical_engineering_done
        country_event = { id = wa_mp_fix_sov_production_bonus.1 }
      }
    }
  }
}

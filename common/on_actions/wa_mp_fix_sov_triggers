on_actions = {
  on_monthly_SOV = {
    effect = {
      if = {
        limit = {
          tag = ITA
          has_completed_focus = SOV_pc_of_mechanical_engineering
          NOT = { has_country_flag = wa_mp_fix_sov_pc_of_mechanical_engineerin }
        }
        set_country_flag = wa_mp_fix_sov_pc_of_mechanical_engineerin
      }
    }
  }
}

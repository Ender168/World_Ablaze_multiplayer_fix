wa_mp_fix_ita_boost_resources = {
  enable = { always = yes }

  icon = GFX_modifiers_increased_metal_production

  # If Italy no longer controls the state, the modifier itself will be deleted.
  remove_trigger = {
    NOT = { is_controlled_by = ITA }
  }

  # state_resource_oil = 10
  # state_resource_aluminium = 10
  state_resource_steel = 150
  # state_resource_coal = 10
  # state_resource_bauxite = 10
  # state_resource_iron = 10
  # state_resource_chromium = 10
  # state_resource_rubber = 10
  # state_resource_tungsten = 10
}

wa_mp_fix_ita_oil_resources_libya = {
  icon = GFX_modifiers_increased_metal_production

  enable = {
    has_global_flag = wa_mp_fix_ita_empire_developed
    OR = {
      is_controlled_by = ITA
      is_controlled_by = ITL
    }
  }

  remove_trigger = {
    has_global_flag = wa_mp_fix_ita_empire_developed
    NOT = {
      OR = {
        is_controlled_by = ITA
        is_controlled_by = ITL
      }
    }
  }
  state_resource_oil = 10
}

